# Local development
# VITE_API_URL=http://localhost:8080

# Production (Amplify): set this to your HTTPS backend origin
# Example: VITE_API_URL=https://api.tudominio.cl
# If you run frontend+api behind same origin (not recommended for Amplify), you can leave it empty.
# VITE_API_URL=
